"use client";

import { SoonPage } from "@/components/dashboard/soon-page";

export default function DocumentsRehydratePage() {
  return (
    <SoonPage
      eyebrow="Documents"
      title="Rehydrate"
      body="Upload the bundle, verify docHash, toggle granted slots. World Selfie and Ledger gates live here."
    />
  );
}
