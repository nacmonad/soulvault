"use client";

import { SoonPage } from "@/components/dashboard/soon-page";

export default function OrgPage() {
  return (
    <SoonPage
      eyebrow="Organization"
      title="ENS profile"
      body="Switcher and read-only ENS records land in the placeholders ticket. Edit stays soon."
    />
  );
}
