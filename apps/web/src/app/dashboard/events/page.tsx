"use client";

import { SoonPage } from "@/components/dashboard/soon-page";

export default function EventsPage() {
  return (
    <SoonPage
      eyebrow="Events"
      title="Chain log"
      body="Kind and tx filters over the shared watcher cache land in the placeholders ticket."
    />
  );
}
