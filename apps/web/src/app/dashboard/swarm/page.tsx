"use client";

import { SoonPage } from "@/components/dashboard/soon-page";

export default function SwarmPage() {
  return (
    <SoonPage
      eyebrow="Swarm"
      title="Membership and epoch"
      body="Members, pending joins, and epoch from useSwarmEvents land next. No join/rotate controls this event."
    />
  );
}
