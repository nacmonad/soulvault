"use client";

import { SoonPage } from "@/components/dashboard/soon-page";

export default function DocumentsGrantsPage() {
  return (
    <SoonPage
      eyebrow="Documents"
      title="Grants"
      body="Wrap selected slot keys to a wallet-attested rehydration key and emit SlotKeyGranted."
    />
  );
}
