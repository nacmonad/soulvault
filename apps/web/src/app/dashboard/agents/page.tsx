"use client";

import { SoonPage } from "@/components/dashboard/soon-page";

export default function AgentsPage() {
  return (
    <SoonPage
      eyebrow="Agents"
      title="ERC-8004 directory"
      body="Agent URI and metadata from useAgentEvents land next. Metadata write stays soon."
    />
  );
}
